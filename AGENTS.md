# Project Instructions

- Use Python 3.11+.
- Keep parser and output/export logic in separate modules.
- Include automated tests for parser and CLI/export outputs.
- Prefer robust parsing heuristics over strict format assumptions.
